upstream web {
  ip_hash;
  server web:8088;
}

# portal
server {

    location /static/ {
        alias /qr/static/;
    }

    location / {
        proxy_pass http://web/;
    }
    listen 8088;
    server_name localhost;
}
